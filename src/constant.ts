class Co {
  static VIEW_WIDTH = window.innerWidth > 0 ? window.innerWidth : screen.width;
  static VIEW_HEIGHT =
    (window.innerHeight > 0 ? window.innerHeight : screen.height) - 30;
  static CHILD_BRANCH_ANGLE_RANGE = 60;
}

export default Co;
